# Embedded ffmpeg resources

Replace placeholders with real static ffmpeg binaries before production use.

Expected names:
- `ffmpeg-macos-arm64`
- `ffmpeg-macos-x64`
- `ffmpeg-windows-x64.exe`

Current tauri config in this workspace packages only `ffmpeg-macos-arm64`.
